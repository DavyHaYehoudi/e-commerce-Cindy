import React from 'react';

const Confirm = () => {
    return (
        <div>
            <button>Valider</button>
            <button>Annuler</button>
        </div>
    );
};

export default Confirm;